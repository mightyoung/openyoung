[project]
name = "agent-coder"
version = "1.0.0"
description = "Coder Agent - OpenYoung"
requires-python = ">=3.10"
dependencies = [
    # 添加你的依赖
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
